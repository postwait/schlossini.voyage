<!DOCTYPE html>
<html>
  <head>
    <% include head %>
  </head>
  <body>
    <div class="jumbotron">
      <div class="container">
        <h1>Schlossini travel journal</h1>
        <p>Follow our travels around the world.</p>
      </div>
    </div>
    <div class="frontcontainer container-fluid">
      <div class="row">
      <% var mdw = 12 / posts.length;
        for(var i=0; i<posts.length; i++) { %>
        <div class="col-md-<%- mdw %>">
          <a href="/traveler/<%- posts[i].person %><% if(dateparam) { %>/<%- dateparam %><% } %>"><div class="author_face">
          <div class="author_image"><img src="/images/profiles/<%= TP.profile_pic(posts[i].date, posts[i].person)%>" /></div>
          </div></a>
        <% (function(post) { %>
        <% include snippet %>
        <% })(posts[i]) %>
      </div>
      <% } %>
      </div>
    </div>
    <%- include footer.ejs %>
  </body>
</html>
